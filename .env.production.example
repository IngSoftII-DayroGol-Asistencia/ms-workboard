# Google Cloud Run Production Environment
# Copy this file to .env.production and adjust values for your deployment

# Database Configuration
# For Cloud SQL with Unix socket (recommended):
# DATABASE_URL=postgresql://user:password@/dbname?host=/cloudsql/PROJECT:REGION:INSTANCE

# For regular PostgreSQL:
DATABASE_URL=postgresql://ms-workboard:\D5:&1Tk]ujI8<Pq@localhost/workboard?host=/cloudsql/deductive-smile-481009-c8:us-central1:ms-users

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=*

# Note: PORT is automatically set by Cloud Run, do not configure it manually
